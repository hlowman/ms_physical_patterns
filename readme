Run order of this repo:
1) make sure macrosheds v2 dataset is dumped into data_working, along with rds metadata files/remap
2) run src/metrics.R to generate annual metrics
3) run src/trends.R to generate trends